#include "main.h"
/**
 * main -Entry point
 * Description:test the case 0 if it will return positive or negative
 * Return: Always 0
 */

int main(void)
{
	printf("entering main\n");
	int i = 0;

	printf("enter test case is  %d in positive or negative function\n", i);
	positive_or_negative(i);
	printf("print after the test case 0\n");
	return (0);
}
